// define addition function with just enough  code to get the test to pass and nothing else
function addition() {
    return 42;
}

// before the test will pass we have to export the function
module.exports = addition;